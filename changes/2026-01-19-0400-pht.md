# 2026-01-19 04:00 PHT - Detailed Change Log

This entry captures the full initial scaffold as of 2026-01-19 04:00 PHT.

## Overview
- Created a monorepo skeleton for a NestJS REST API and Android Compose app.
- Added Prisma with a placeholder schema and JWT module wiring.
- Added Render deployment config and basic setup instructions.

## Root
- `package.json`
  - Declared npm workspaces for `apps/api` and `packages/shared`.
  - Added root scripts that proxy to `apps/api` for dev/build/start and Prisma commands.
- `README.md`
  - Documented prerequisites (Node 18+, Android Studio, Postgres connection).
  - Included API run/build steps, Prisma generate/migrate, and Android Studio usage.
  - Noted base URL placeholder and environment variable setup.
- `.gitignore`
  - Ignored `node_modules`, `apps/api/dist`, `apps/api/.env`, Prisma migrations.
  - Ignored Android build outputs, Gradle cache, `local.properties`.
  - Ignored IDE folders and OS files.
- `render.yaml`
  - Added Render web service config for Node 18.
  - Build command runs `npm install`, Prisma generate, and API build.
  - Start command runs `npm run start:api` with `PORT` env.
- `scripts/bootstrap.ps1`
  - Copies `apps/api/.env.example` to `apps/api/.env` if missing.
- `CHANGELOG.md`
  - Added living change log with summary entry for this batch.

## API (`apps/api`)
- `apps/api/package.json`
  - Added NestJS core, config, JWT, and platform dependencies.
  - Added Prisma client and supporting TS dependencies.
  - Scripts for dev (`nest start --watch`), build, prod start, and Prisma commands.
- `apps/api/nest-cli.json`
  - Configured Nest CLI with `src` as the source root.
- `apps/api/tsconfig.json`
  - Configured TS compiler for NestJS output in `dist`.
  - Set `strict` to `false` to keep scaffolding minimal.
- `apps/api/tsconfig.build.json`
  - Build config excluding tests and build outputs.
- `apps/api/.env.example`
  - Placeholder env vars: `DATABASE_URL`, `JWT_SECRET`, `PORT`, `NODE_ENV`.
- `apps/api/prisma/schema.prisma`
  - Configured Postgres datasource and Prisma client generator.
  - Added placeholder `Example` model with `id`, `note`, `createdAt`.
  - Commented TODO for User and other domain models.
- `apps/api/src/main.ts`
  - Bootstrapped Nest app and enabled global `ValidationPipe`.
  - App listens on `process.env.PORT` or 3000.
- `apps/api/src/app.module.ts`
  - Loaded `ConfigModule` as global.
  - Imported `PrismaModule`, `HealthModule`, `AuthModule`.
- `apps/api/src/health/health.module.ts`
  - Declared the health controller.
- `apps/api/src/health/health.controller.ts`
  - Implemented GET `/health` returning `{ status: 'ok' }`.
- `apps/api/src/auth/auth.module.ts`
  - Registered `JwtModule` using `JWT_SECRET` with fallback `CHANGE_ME`.
  - Added `AuthController` and `AuthService`.
- `apps/api/src/auth/auth.controller.ts`
  - Declared `POST /auth/register`, `POST /auth/login`, `GET /auth/me`.
  - Wired endpoints to `AuthService`.
- `apps/api/src/auth/auth.service.ts`
  - Stubbed `register`, `login`, `me` with `{ status: 'NOT_IMPLEMENTED' }`.
- `apps/api/src/auth/dto/register.dto.ts`
  - Added minimal validation for `email` and `password` (min length 6).
- `apps/api/src/auth/dto/login.dto.ts`
  - Added minimal validation for `email` and `password`.
- `apps/api/src/prisma/prisma.module.ts`
  - Registered `PrismaService` globally.
- `apps/api/src/prisma/prisma.service.ts`
  - Connected Prisma on module init.
  - Added a shutdown hook helper (`enableShutdownHooks`).

## Shared (`packages/shared`)
- `packages/shared/package.json`
  - Minimal shared package placeholder metadata.
- `packages/shared/contracts.md`
  - Listed intended endpoints without schemas.

## Android (`apps/mobile`)
- `apps/mobile/settings.gradle.kts`
  - Set project name and included `:app`.
- `apps/mobile/build.gradle.kts`
  - Declared Android application and Kotlin Android plugins.
- `apps/mobile/gradle.properties`
  - Set JVM args and AndroidX flags.
- `apps/mobile/gradle/wrapper/gradle-wrapper.properties`
  - Set Gradle wrapper distribution URL to 8.6.
- `apps/mobile/gradlew`
  - Added Gradle wrapper shell script (no wrapper jar included).
- `apps/mobile/gradlew.bat`
  - Added Gradle wrapper batch script (no wrapper jar included).
- `apps/mobile/app/build.gradle.kts`
  - Configured Android app namespace, SDK levels, and version.
  - Enabled Compose and set compiler extension version.
  - Added Java/Kotlin target 17.
  - Added Compose, navigation, Retrofit, Moshi, and OkHttp deps.
- `apps/mobile/app/src/main/AndroidManifest.xml`
  - Declared application label, theme, and `MainActivity` launcher.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/MainActivity.kt`
  - Compose entry activity that renders `AppNav()` inside theme.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/AppNav.kt`
  - Navigation with routes: Login, Register, Home.
  - Wiring actions to navigate between screens.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/AppConfig.kt`
  - Base URL placeholder `http://10.0.2.2:3000/`.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/data/network/ApiClient.kt`
  - Retrofit client with Moshi converter and OkHttp.
  - Auth endpoints: register/login/me with `Response<Unit>` stubs.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/ui/screens/LoginScreen.kt`
  - Placeholder UI with buttons for login/register navigation.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/ui/screens/RegisterScreen.kt`
  - Placeholder UI with buttons for register/login navigation.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/ui/screens/HomeScreen.kt`
  - Placeholder UI with logout button.
- `apps/mobile/app/src/main/java/com/wheelsongo/app/ui/theme/Theme.kt`
  - Minimal Material3 theme wrapper.
- `apps/mobile/app/src/main/res/values/strings.xml`
  - App name string resource.
- `apps/mobile/app/src/main/res/values/themes.xml`
  - Material3 theme style definition.
