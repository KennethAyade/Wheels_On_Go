{
  "name": "wheels-on-go",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev:api": "npm --prefix apps/api run start:dev",
    "build": "npm --prefix apps/api run build",
    "build:api": "npm --prefix apps/api run build",
    "build:prod": "npm --prefix apps/api run build:prod",
    "start:api": "npm --prefix apps/api run start:prod",
    "prisma:generate": "npm --prefix apps/api run prisma:generate",
    "prisma:migrate": "npm --prefix apps/api run prisma:migrate",
    "prisma:studio": "npm --prefix apps/api run prisma:studio",
    "test:api": "npm --prefix apps/api test",
    "seed:admin": "npm --prefix apps/api run seed:admin",
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build"
  }
}
